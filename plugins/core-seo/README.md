# Core SEO

Core plugin for JessCMS search engine optimization.

## Provides

- `seo_title` and `seo_description` fields on content types
- Sitemap generation (future)
- Global SEO defaults via plugin settings

## Status

Phase 2 — manifest only. SEO fields exist in schema; sitemap not implemented.
